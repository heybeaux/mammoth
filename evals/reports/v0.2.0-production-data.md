# v0.2.0 Production Data Checkpoint Receipt

## Verdict

P2 passed on 2026-07-10 (America/Vancouver). The verified implementation commit is
`a99c4fb1c0560e52838ddee6b195ddb35adecf88`, the `main` merge of PR #25. The
subsequent checkpoint PR changes only this receipt and status documentation. Tag
`v0.2.0-production-data` is to point at that receipt merge after default-branch CI
passes.

Merged delivery PRs: #18 roadmap, #19 adapter contracts, #20 conformance and
Observatory contract, #21 autonomous P2 loop, #22 P1 freeze and D1, #23 D2/D3/D6,
#24 D4, and #25 D5.

## Environment and contracts

- Node `v22.22.2`; pnpm `8.15.6`.
- Local verification used PostgreSQL server `18.0`; CI independently passed with
  PostgreSQL `16.14` on Ubuntu.
- Adapter contract major: `1`. Workspace implementation packages remain internal
  version `0.0.0`: `@mammoth/postgres-adapter`, `@mammoth/production-cas`,
  `@mammoth/production-profile`, and `@mammoth/observatory-projection`.
- Migration checksums:
  - v1 `800c8be6079e658336fc3eaa842f9483c0157bd325062b81efaecc4ef1a6622f`
  - v2 `db01f591ff0ef5cbc7bc49388a145a242713d9ca4e46c0099cd4e83f8735ea7d`
  - v3 `ae4385dc968fa0484b6c151fddcdb93a3f81d8c75b8256bca59778f62e7ef9c3`

## Executed evidence

The complete repository format, lint, typecheck, test, and build ladder passed
before PR #25. GitHub Actions run `29141523726` passed in 2m56s, including the real
production-profile lifecycle and backup/restore gate. Local `pnpm verify:p2`
passed every gate in 28.4s:

1. P1 adapter contract freeze and local conformance.
2. D1 empty/upgrade/interruption/checksum/restart migrations.
3. D2 transactional ledger concurrency, rollback, references, audit/outbox.
4. D3 CAS dedupe, tamper/truncation rejection, orphan reconciliation.
5. D4 fencing, cancellation, crash windows, duplicate dispatch, retry/poison rows.
6. D5 bounded startup/readiness, immediate process stop, restart, persistence.
7. D5 `pg_dump`/`pg_restore` into a distinct database plus CAS copy and integrity.
8. D6 deterministic Observatory projection, completeness, digest, and provenance.

The restored integrity manifest contained ledger revision 1; one audit row; one
authoritative outbox row; one completed work item; one completed provider-effect
receipt; two work-outbox rows; all three migration checksums; and one verified CAS
artifact with digest
`sha256:370c0262ab84beda18603dd44036bef38de5a5973832059cd7009c4e601d76ff`.

Injected recovery boundaries included unready startup, bounded clean shutdown,
immediate PostgreSQL termination, restart against persistent storage, duplicate
provider/outbox delivery, stale fencing tokens, transactional rollback, poison
rows, CAS tampering/truncation, and restore into a fresh database.

## Observatory and limitations

The checked projection fixture is
`evals/fixtures/p2/observatory-projection.json`, file SHA-256
`54e75d067f64d519bafc98617b28e80fccf6b343ea1c5d489139ec715afe9ad3`.
It is versioned, deterministic, read-only, provenance-preserving, and contains
explicit completeness/omission metadata.

Deferred: managed Postgres/object-store deployment adapters, Temporal workflow
execution, broader research-cell orchestration, and the 3D Observatory UI. These
must consume the frozen ports and cannot invent authority beyond this projection.
