{
  "name": "@mammoth/mvp-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@mammoth/runtime": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "verify": "tsx src/cli.ts"
  }
}
