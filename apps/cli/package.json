{
  "name": "@mammoth/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "mammoth": "./dist/bin.js"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && esbuild src/bin.ts --bundle --splitting --platform=node --format=esm --target=node22 --external:@temporalio/* --external:@grpc/* --outdir=dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mammoth/runtime": "workspace:*",
    "@mammoth/temporal-adapter": "workspace:*",
    "@mammoth/workflow": "workspace:*",
    "@temporalio/client": "1.20.2",
    "@temporalio/workflow": "1.20.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
